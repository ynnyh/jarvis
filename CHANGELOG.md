# 更新日志

记录每个版本的新增、优化和修复。版本号遵循语义化版本（major.minor.patch）。

## v0.10.4

### 修复
- **小人拖拽只能停在下半屏（macOS）**：拖拽是小人唯一依赖浏览器鼠标屏幕坐标的交互，透明无边框窗口上该坐标的量纲与系统逻辑屏不一致，导致位移被压缩、上半屏怎么拖都够不到。改为统一从系统真实鼠标位计算窗口落点，彻底不再依赖浏览器屏幕坐标。
- **混合多屏下小人点不动、拖不动（macOS）**：接了外接显示器（如 Retina 笔记本 + 普通外屏）时，鼠标命中判定的坐标换算把「主屏缩放」与「窗口所在屏缩放」混用，命中点算到小人框外，导致窗口空白区的穿透判定一直生效、小人收不到任何点击与拖拽。改为两个缩放各自换算后再相减，单屏与同缩放多屏行为不变。
- 移除上一版遗留的 macOS 穿透临时诊断代码。

## v0.10.3

### 修复
- **工时事务类下拉透明穿透**：手动工时窗口选「事务」类工作后弹出的任务下拉、写工时窗口的任务搜索下拉，背景用了只有 3% 透明度的 `--surface`，且阴影引用了不存在的 `--overlay` token 导致阴影失效，下拉跟后面内容互相穿透。改为不透明的 `--popup-bg` + `--panel-shadow`，与批量写工时下拉的现成写法对齐。

### 诊断（macOS）
- **欢迎页整窗穿透排查**：macOS 上欢迎页（及整个小人窗口）无法点击、鼠标穿透到桌面。根因锁定在 `cursor_pos_in_window` 的坐标换算——v0.5.4 的「cursor 当 logical」前提与 tao 0.35.2 源码（末尾 `.to_physical(scale)`）矛盾，retina 屏（M 芯片）算出的 CSS 坐标落到窗外，前端误判「鼠标不在 UI 上」于是 `setIgnoreCursorEvents(true)` 整窗穿透。本版**临时**统一走 `(cursor-win)/scale` 公式，并让后端多返回 5 个原始 OS 坐标、前端在「判窗外穿透」时打 `[passthrough][diag]` 日志，供真机读数定夺最终公式。确认完会回退成干净版。

## Unreleased

### 开源准备
- **安全脱敏**：清理生产代码与 git 历史中的内网地址、公司业务信息（禅道/帆软/Jenkins 相关），统一替换为通用示例或 RFC 5737 文档 IP。重写全部 git 历史确保无残留。
- **移除内部开发笔记**：`.trellis/` 目录（开发期任务规划/spec）移出开源仓库。
- **新增 SECURITY.md**：安全漏洞披露通道 + 本地优先架构的安全说明。
- **修复 dev.mjs 启动**：`npx tauri dev` 改为直接调用本地 `@tauri-apps/cli`，避免误拉同名占位包；同时消除 `shell:true` 安全告警。
- **新增项目级 `.npmrc`**：`include=dev` 对抗 `NODE_ENV=production` 导致 devDependencies 被跳过的问题。
- **CI 双源发布**：release.yml 新增 GitHub Releases 发布步骤（此前仅发 Gitee）。
- **文档完善**：README 补充应用截图；官网首页 hero 图换为真实桌宠截图，新增界面预览画廊。

## v0.10.2

### 修复
- **自定义宠物切换不显示**：切换或新增自定义宠物后，桌面小人总是回退为默认机器人。根因是头像主窗口与设置窗口是独立窗口、各自持有独立缓存；已改为响应式缓存，并在配置变更时同步刷新。
- **自定义宠物编辑保存失效**：点击「编辑」后保存无响应。已重构保存逻辑，不再依赖组件初始化时机。
- **自定义宠物编辑动画不生效**：编辑修改动画效果后桌面形象不更新。新增独立的宠物数据变更事件，确保各窗口及时刷新缓存。
- **自定义宠物选择与删除**：修复选择自定义宠物不生效；删除改为应用内二次确认，不再弹系统原生框。
- **更新界面卡死**：检查更新或下载时若网络卡顿、macOS 安装阶段 hang 住，更新窗口会锁死、无法关闭。已为检查加 30s 超时，并允许在检查 / 下载阶段关闭窗口逃离。

## v0.10.1

### 新增
- 自定义宠物上传：支持上传 Lottie JSON、PNG、JPG、GIF 格式作为自定义宠物形象，可在设置中管理（上传/编辑/删除）。
- 自定义下拉组件：全局替换原生 select，所有下拉框完全跟随主题切换。

### 优化
- 图片宠物圆形裁剪显示，更美观。
- 图片宠物新增旋转、弹跳动画效果（共 4 种：呼吸、摇摆、旋转、弹跳）。
- 模态框点击外部不再关闭，取消时清空数据，避免误操作丢失上传内容。
- 删除正在使用的自定义宠物时自动回退到默认机器人形象。
- 启动时验证宠物形象有效性，无效 ID 自动回退默认。

## v0.10.0

### 新增
- 对话式发版：在聊天里说「给某项目测试环境发版」，Jarvis 会自动匹配 Jenkins 任务、读取构建参数、生成确认卡片，点击确认后才真正触发构建，并自动轮询回报构建状态（成功 / 失败 / 超时）。支持配置多个 Jenkins 账号、每个账号下挂多个项目。

### 优化
- 发版配置设置页重做：改为账号卡片式，用户名带「Jenkins User ID」说明；测试连接直接验证当前填写的地址 / 用户名 / 令牌，无需先保存。
- 删除对话改为应用内二次确认，不再弹系统原生确认框。

### 修复
- 消除保存发版配置时弹出的控制台黑框（Windows）。
- 修复 Jenkins 连接中断后卡死、需重启应用的问题（自动重连重试）。
- 修复触发发版后「构建状态查询出错」。

## v0.9.1

### 修复
- **帆软报表拉取**：修正 CID 校验和算法（复刻前端 fingerprintHandle 的 MD5 校验），修复 read_w_content 返回 Invalid_CID / 空响应的问题
- **新任务弹窗**：宠物 dock 隐藏状态下来新任务时先恢复位置再弹出，避免弹窗被截断、按钮不可见
- **帆软响应解析**：增强空 body 和 HTML 错误页检测，触发重登重试而非 JSON 解析崩溃

### 优化
- **设置页开关**：全部勾选框统一改为胶囊开关（ToggleSwitch），覆盖 9 个组件共 17 处
- **成本功能入口**：项目成本开关从「禅道」页面移到「常规」页面，和其他功能开关放一起

## v0.9.0

### 新增
- **语音输入工具**：按热键说话，转写文字注入当前聚焦输入框；支持中英混说，自动断句标点
- **本地语音引擎（SenseVoice）**：基于 sherpa-onnx + FunASR SenseVoice 离线识别，隐私不上云，首次需下载模型约 250MB
- **云端语音引擎（豆包·火山）**：接入火山引擎流式 ASR，快且准、中文强，有免费额度；本地/云端可切换
- **自定义热键**：设置内录制改键，支持 Ctrl/Cmd + Shift/Alt + 主键组合
- **LLM 客户端 Anthropic 协议**：接入 Claude Messages API，修 claude 模型导入与流式死锁
- **发版配置后端**：读写预设/连接、token 进密钥链、保存后重启 jenkins-mcp
- **发版配置设置页**：前端配置界面，默认隐藏开关
- **对话式发版确认卡片**：前后端闭环，发版前在对话中确认
- **MCP 工具注入 agent**：通用 MCP stdio client（rmcp 1.7），工具动态安全分类

### 优化
- **设置页勾选框**：全部改为胶囊/药丸式滑动开关，统一现代风格
- **语音引擎选择器**：改为 macOS 风格分段控件，选中项高亮
- **语音转写**：拉满线程提速、锁中文、术语词库、识别中反馈
- **语音资产下载**：走代理 + 断点续传/重试 + 手动兜底区 + 国内镜像加速

### 修复
- **语音输入全链路诊断**：失败可见化，定位采集为空等问题
- **聊天窗与桌面小人共存**：打开聊天窗不再隐藏桌面小人
- **帆软 JWT 过期**：自动清缓存重试
- **手动添加条目**：支持模糊搜索选择禅道任务
- **极简主题**：下拉菜单透明度与按钮样式修复、更新面板透视修复

## v0.8.3

### 新增
- **前端风格主题系统**：新增 6 套视觉风格（霓虹/俏皮/治愈/极简/矩阵/赛博），设置页卡片式选择器，切换即时生效、跨窗口同步、重启持久化
- **Pulsefire 能量粒子特效**：赛博主题粒子升级为未来战士风格，支持运动拖尾、粒子连线能量网络、脉冲突发、速度呼吸曲线
- **工作内容质量检测**：一键写入前检测工作内容不足 12 字的条目，弹出警告提示补充细节

### 修复
- **子窗口主题不生效**：补全 BatchWrite/WriteHours/TodayPlan/ManualHours 四个子窗口的 configStore.load()，主题切换 now 全窗口生效
- **单条 commit 编号**：只有一条 commit 时工作内容不再加"1、"前缀

## v0.8.1

### 新增
- **成本分析帆软数据源**：项目成本数据改用帆软报表数据源，渠道机器人新增 cost_report 工具支持对话查询成本
- **成本加班拆分**：成本分析勾选后拉取工作日志，按日期拆分计算加班工时

### 修复
- **一键写入格式**：工作内容改为数字编号一段话
- **一键写入选中回填**：修复 orphan 条目 kind 未切换导致选中任务后未回填的问题

## v0.8.0

### 新增
- **项目成本分析窗口**：独立大窗展示团队成本，支持项目搜索、概览卡片、人均工时条形图、成本占比饼图、明细表（内联编辑中文名和时薪）
- **时薪配置**：设置页新增时薪配置区，支持按项目拉取团队成员、统一时薪批量设置、自动保存
- **机器人成本查询**：新增 cost_report 工具，机器人可通过对话查询项目成本；cost_report_preview 轻量预览用于二次确认项目名
- **右键菜单入口**：4 套主题均增加「项目成本」入口

### 优化
- **禅道数据源**：成本数据从禅道任务 team 字段聚合，不依赖帆软报表

## v0.7.6

### 新增
- **复盘内联写工时**：复盘大窗内直接展开写工时表单，不再弹小窗；支持输入关键词模糊搜索禅道任务并点选
- **复盘写任务入口**：复盘顶部新增「写任务」按钮，可搜索任意禅道任务后直接写工时
- **需更新状态任务写工时**：「需更新状态」区每条任务新增「写工时」按钮，taskId 预填
- **Chat 流式输出**：Chat 支持 SSE 流式实时输出，消息体使用 Markdown 渲染 + 代码高亮
- **渠道推送晚间复盘**：晚间复盘可同时推送到手机渠道（Telegram/QQ），配置页增加开关

### 优化
- **日报格式**：纯文本日报改用数字编号紧凑段落，不再用横杠拼接
- **工时摘要**：写工时确认摘要中显示任务名称，不再只显任务 ID
- **通知定时器重构**：统一为 shared tick 模式，简化各通知 composable

## v0.7.5

### 重构
- **tools.rs 拆分**：1768 行单文件拆分为 10 个领域模块
- **fine_report 拆分**：精细化报表模块按子功能分目录
- **channels/router 拆分**：渠道路由模块按子功能分目录
- **git_scan 模块化**：git 扫描模块拆分为多文件目录

### 新增
- **发版预检脚本**：pre-release.mjs 自动检查版本一致性、CHANGELOG、cargo check 等
- **发版工具**：release 相关工具脚本

## v0.7.4

### 新增
- **长周期工时工作汇报**：新增完整的长周期工时汇报能力，支持按本月、本季度、近半年、本年，以及自定义超过 7 天的区间生成完整工作汇报正文和数据附录。

### 优化
- **工时统计入口统一**：桌面端「设置 → 工时统计」升级为双模式查询，短周期默认展示工时明细，长周期自动切换为工作汇报，与机器人回复规则保持一致。
- **复盘页概览布局**：今日复盘顶部概览卡片改为按实际卡片数量自动铺满，避免 3 张卡时右侧留下空白。
- **贴边小人交互**：贴边后悬停改为沿当前边缘展开，并完整露出小人，不再跳回贴边前的历史位置。
- **设置与引导体验**：整理了工作模式、主动提醒、工时收尾提醒、工作时段小提示、设置菜单与首次引导页的文案和结构，整体更清晰。

### 修复
- **机器人长周期工时问答错误路由**：修复 QQ / Telegram 渠道里“本季度工时”“近半年工时”“今年工时”等请求仍掉回明细查询的问题。
- **自定义区间模式判定**：修复自定义时间范围被粗暴归为长周期汇报的问题，现在 7 天及以内保持明细，超过 7 天才切汇报模式。
- **设置链路乱码**：清理设置详情页、设置小菜单、今日计划、首次引导等可见区域的乱码和不一致文案。
- **多模式映射错误**：修复 `workStyle` 在今日计划和设置页中的映射不一致问题，`focused / multi / transactional / balanced` 现在都能正确显示。

## v0.7.3

### 修复
- **设置页更新日志显示为空**：改用 Vite define 在构建时读取 CHANGELOG.md 并内联到 bundle，避免跨 root 的 `?raw` import 在不同环境下的行为差异。
- **代码健壮性（续 v0.7.2）**：完成前端健壮性排查（渠道重启时序、去重 Set 清理、onFocusChanged unlisten 清理、消息列表 key 用 createdAt、移除泄露任务名的 console.log）。

### 优化
- **写工时体验**：Ctrl/Cmd+Enter 提交，打开窗口自动聚焦首个待填框。
- **复盘体验**：写工时失败显示错误提示，防止连点开多个窗口。
- **帆软工时查询**：日期预设 tab 高亮当前档。
- **日常复盘**：孤儿工时求和增加 NaN 过滤。

## v0.7.2

### 修复
- **全量代码健壮性排查**：修复一批生产隐患（详见下方）。
- **发布到 Gitee 时 ETIMEDOWN 未触发重试**：增加网络错误重试机制。

### 系统级修复（Rust 后端）
- **PANIC 根治**：release 是 panic=abort，中文多字节 UTF-8 字节切片会杀进程且 debug 不暴露；新增 `util::truncate_chars` 替换 25 处危险切片。
- **存储损坏防护**：所有 JSON 写盘改原子写（临时文件 + rename），防写一半进程退出留残文件。
- **并发写安全**：config 并发加 `CONFIG_WRITE_LOCK` 串行化，防设置面板与机器人互相覆盖。
- **禅道高危修复**：`add_effort` 缺 `left` 字段改报错中止（回填 0 会被禅道自动关任务）。
- **消息丢包加固**：telegram offset 推进挪到 send 成功后 + 退避重试；qqbot 即时心跳 + token 缓存 + 失败清缓存。
- **LLM 防护**：429 重试 + agent 循环上限 + 工具黑名单（agent 不准自调 log-task-effort）。
- **命令安全**：`settings_open` page 参数走 ascii 白名单防 JS 注入；多处 `as_object_mut().unwrap()` 改 `?`；日志脱敏（debug 包裹 + 去明文）。

### 前端修复
- **时序与清理**：渠道重启防 telegram 409（250→500ms）；去重 Set 清理防内存增长；`onFocusChanged` unlisten 进 `onUnmounted`。
- **消息列表渲染**：key 从 index 改 `msg.createdAt` 防切换对话 DOM 复用串状态。

## v0.7.1

### 新增
- **自动关闭面板**：点击外部或窗口失焦时自动关闭浮层和菜单，减少手动关闭操作。

## v0.7.0

### 优化
- **CircleCI 发版流水线**：新增 Node.js、Rust、npm、cargo 构建缓存，后续构建大幅加速。
- **Windows 构建资源升级**：executor 从 medium 升级到 large，更多 CPU 和内存。

### 修复
- **CircleCI 签名密钥验证失败**：构建步骤直接读取 CircleCI 项目级环境变量，不再手动 unset 导致私钥丢失。

## v0.6.5

### 修复
- **内部 macOS 开发包错误触发 Apple 公证**：没有 Apple 证书时不再向 Tauri 传 `APPLE_ID` / `APPLE_PASSWORD` / `APPLE_TEAM_ID`，避免打包时报 `Team ID must be at least 3 characters`。

## v0.6.4

### 新增
- **手动写工时**：单页布局，任务按四类（运维 / 事务 / 新增功能 / 其他）分 Tab，运维下额外按项目（任务名前缀）二级筛选，任务支持模糊搜索下拉，工时和内容直接在同一页填写。
- **更新日志**：每次有新版本时弹窗会展示本次更新内容；在「设置 → 关于与更新」里可以查看历史所有版本。

### 优化
- **大窗外观统一**：写工时和手动写工时两个独立窗口去掉系统原生白色标题栏，自定义右上角关闭按钮（hover 红色），与聊天/设置窗口保持一致风格。

### 修复
- **写工时窗口右上角关闭后小人消失、再开模态框无反应**：原生关闭按钮默认销毁窗口实例导致下次找不到 webview，同时关联状态没恢复。改为 Rust 层拦截关闭事件，只隐藏窗口，不销毁。
- **macOS 下载后提示应用已损坏**：发布流水线改为构建 Universal macOS 包；内部开发包新增安装脚本，自动复制到 `/Applications` 并清理 quarantine；更新器同时登记 `darwin-aarch64` 与 `darwin-x86_64`。

## v0.6.3

### 新增
- **聊天渠道接入**：Telegram、QQ Bot 双向消息，未来对话式协作的入口。

## v0.6.2

### 新增
- **Lottie 宠物头像**：替代静态图，动态表情。
- **时段问候**：根据当前时间自动切换问候语和表情。
- **贴边收纳**：小人靠近屏幕边缘时自动收起，留出工作区。

## v0.6.1

### 修复
- 自动更新检查在某些环境下因私有字段访问报错。
- 写工时窗口缺权限配置导致部分操作静默失败。
- 小人在全屏应用上不能被拖动。
- 设置等浮层方向不会根据小人位置自适应。

## v0.6.0

### 新增
- **任务与代码仓库绑定**：commit 自动归属到正确的禅道任务，LLM 智能判断归属。
- **一键写工时**：复盘窗口里看到 commit，弹窗确认就能写入禅道。

### 修复
- 仓库目录绑定时正确展开真实 git 仓库路径。
- 应用业务线排除规则到 commit 列表过滤。
