# 强制安装 devDependencies
# 项目依赖了 @tauri-apps/cli / vitepress / typescript 等 devDependencies，
# 但开发机若设置了 NODE_ENV=production，npm 会默认 omit=dev 跳过它们，
# 导致 tauri / vitepress 命令找不到，npx 误拉同名占位包。
# 此处 include=dev 确保任何环境下都安装 dev 依赖。
include=dev
