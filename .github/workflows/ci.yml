name: CI

# 质量门：push 到 main 或提 PR 时跑。实际检查逻辑在可复用的 quality.yml，
# 发版 release.yml 也调同一个 workflow，保证「CI 跑的」和「发版前置跑的」完全一致。
on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    uses: ./.github/workflows/quality.yml
